# Test-playbook — [progetto]
> Versione leggibile di flow.config.testPlaybook. La fonte di verità operativa resta il JSON;
> questo file serve all'umano per capire e rivedere. Catturato in intervista, NON inferito.

## [tipo di test, es. non-regression / data-diff]
- Quando si applica: [condizione, es. "modifiche a sync/, schema DB, query che scrivono su tabelle"]
- Come si lancia: [comando]
- Note: [es. richiede snapshot 'before' via gate pre-work-snapshot]

## [tipo di test, es. end-to-end]
- Quando si applica: [es. "modifiche a UI/componenti/pagine"]
- Come si lancia: [comando]
